//function translate(text) {
function makeText(inputText) {
    console.log('I funksjon: ', inputText);
    show();
 }
/*function makeText(tekst) {
    console.log('I funksjon: ', document.getElementById('inputs').value);
    for (let i = 0; i < tekst.length; i++) {
    inputText = tekst[i];
    return translate(inputText);
 }
}

//console.log('Sjekker funksjonen translate= ',translate('b'),translate(' '),translate('a'));
//if(JSON.stringify(morseCodeJSON)==JSON.stringify(annenArray)) */
