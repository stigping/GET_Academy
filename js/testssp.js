QUnit.test( "Random Number?", function( assert ) {
    cpuChoose = randomNum(1);
    assert.equal(ranNum, true);
  });
  QUnit.test( "Random Number?", function( assert ) {
    cpuChoose = randomNum(0);
    assert.equal(ranNum, true);
  });
  QUnit.test( "Random Number?", function( assert ) {
    cpuChoose = randomNum(2);
    assert.equal(ranNum, true);
  });