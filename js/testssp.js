// QUnit.test( "Random Number? test test", function( assert ) {
//     let ranTest = null;
//     ranNum = ranTest;
//     if (ranTest ==     
//       ) ranTest = true;
//     assert.equal(ranTest, true);
//  });

//  QUnit.test( "Random Number?", function( assert ) {
//     const isValid = randomNum(1);
//   assert.equal(isValid, 0 ? 1 : 2);
// });

QUnit.test( "Random Number test", function( assert ) {
  let maxNum = 3;
  let test = randomNum() < maxNum ? true : false; 
assert.equal(test, true);
});

QUnit.test( "CPU TesterTester", function( assert ) {
  let testTall = choice.length();
  let testTell = cpuChoose
  assert.equal(testTell < testTall)
});

QUnit.test( "CPU Test 2", function( assert ) {
  let test = cpuChoose() === 'rock' ? true : false;
assert.equal(test, true);
});